import { toggle } from "@tailus/themer-toggle";

const toggleGroup = {
    root: "flex items-center justify-center gap-px",
    item: toggle.root,
    icon: toggle.icon,
    iconStateOn: toggle.iconStateOn,
    iconSize: toggle.iconSize,
};

export default toggleGroup;
export { toggleGroup };
