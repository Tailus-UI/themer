import { Shade } from "../types";

export type SelectConfig = {
    /**
     * The background of the Select component on dark mode.
     */
    dark?: {
        /**
         * The background of the Select component on dark mode.
         */
        bg?: Shade;
    };
};

export default SelectConfig;
